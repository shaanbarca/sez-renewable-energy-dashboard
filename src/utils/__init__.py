"""Project-wide utility modules.

Currently exports:
- `provenance` — field-level data provenance tracking (source / vintage /
  confidence / citation). Used by the v4.1+ cost framework so every numeric
  output is auditable to a primary source.
"""
