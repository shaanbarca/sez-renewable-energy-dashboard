# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

### Added
- **MS GMLBF integration — Phase 5 (integrated rebuild + first results).** End-to-end multi-source pipeline now live. GoB v3 (337k buildings) merged with Microsoft GMLBF (268k buildings) via per-site STRtree IoU dedup at threshold 0.5 → 549k merged → 292k after fence-boundary polygon clip. Total rooftop nameplate ceiling lifted **2,396 → 2,743 MWp (+14.5%)**. RV-eval actionable findings dropped **11 → 7 (-36%)**; zero-MWp specifically **7 → 2 (-71%)**. **5 of 7 RV7 cases unblocked**: Cemindo Gemilang Bayah (0 → 10.74 MWp), Pupuk Iskandar Muda (0 → 13.84), SBI Andalas Lhoknga (0 → 5.94), Buli IP (0 → 2.63), IPIP (0 → 1.41). 2 remaining: Red Lion Hongshi Tonga + IWIP — both post-2023 buildouts where GoB AND MS imagery predate plant commissioning, need centroid hunt (Hongshi has wrong centroid per cemnet.com) or imagery refresh. New cross-source eval check `ms_gmlbf_reveals_buildings` (🟢 info-tier) surfaced 54 sites where MS materially expanded GoB coverage. METHODOLOGY §4A.7a updated with phase status table + per-site results.
- **MS GMLBF integration — Phase 1 (download scaffolding).** New `scripts/download_microsoft_buildings.py` fetches Microsoft Global ML Building Footprints tiles for Indonesia. Dry-run verified: 601 tiles, ~4.7 GB gzipped (20–40 min on standard fiber, 4 workers). Resumable; uses `data/microsoft_buildings/raw/` (gitignored). This is RV7's structural fix — addresses the 7 `zero_mwp_with_capacity` findings (Cemindo Bayah, Hongshi, IWIP, IPIP, Buli, PIM, SBI Andalas Lhoknga) where GoB v3 simply doesn't detect plants that are clearly visible on satellite, plus the post-2023 buildout gap (GoB imagery cutoff is May 2023). The 6 forward-compat invariants in spec §13.10 (shipped at v4.1) make this an additive PR — Phase 2 is a preprocess script + new `src/pipeline/building_sources/ms_gmlbf.py` module + dedup-by-IoU in `load_buildings_for_pipeline()`. Methodology §4A.7a documents the phased delivery plan + targets.

### Changed
- **RV-eval flag clarity + history relocation.** Rewrote `scripts/eval_rooftop_accuracy.py` output to group findings by what's wrong instead of by HIGH/LOW priority. Six distinct flag types — `zero_mwp_with_capacity`, `sector_outlier_above` / `sector_outlier_below` (split from the old `sector_ratio_outlier` so direction = root cause), `footprint_below_band`, `footprint_above_band`, `sector_sample_too_small` — each rendered with an icon (🔴 actionable / 🟡 review / ℹ info), a one-line rule, and a one-line suggested fix. The user can now scan the report and know what each finding needs without decoding a vague "HIGH" label. Priority enum in JSONL is also semantically meaningful (`actionable` / `review` / `info`); only `actionable` triggers exit 1. Trend log moved from `~/.gstack/projects/eez/eval-history.jsonl` (outside the repo, easy to lose) to `outputs/data/eval/rooftop-history.jsonl` — generated artifact, lives with other pipeline outputs. Existing 3 history records preserved. `EVAL_HIST_PATH` in `src/assumptions.py` updated to the repo-relative path; `append_history()` resolves it against `REPO_ROOT` so cwd doesn't matter. Re-run on current pipeline: 11 actionable + 5 review + 1 info (down from "16 HIGH" — same underlying findings, footprint-band hits demoted to review since they're warnings about already-flagged sites, not net-new failures). Tests: 18 (added 2 — direction split + format groupings). Methodology §4A.8 + spec §16.6 + CLAUDE.md updated.
- **`scripts/visual_sanity_check.py` is now eval-driven.** Dropped the hardcoded SUSPECT_SITES list (11 manually-curated sites with author commentary). The script now reads the latest record from `outputs/data/eval/rooftop-history.jsonl` and renders one satellite + detected-polygon image per flagged site, with all of that site's findings shown in the image title (e.g. Cemindo Bayah's `zero_mwp_with_capacity` AND `footprint_below_band` show together on one image). Single source of truth: when the eval flags a site, it gets a visual; when it stops flagging, the visual is no longer regenerated. `--site-ids a,b,c` CLI flag preserved for ad-hoc rendering of baselines or fix-targets that aren't currently flagged. ASCII tokens (`[!]` / `[?]` / `[i]`) used in image titles instead of emoji because matplotlib's default font doesn't render the 🟡 glyph. First eval-driven run: 15 site images (11 actionable + 4 review with site_id; the sector-level `sector_sample_too_small` finding is correctly skipped — no site to render).

### Added
- **V4.1 automated rooftop accuracy eval.** New `scripts/eval_rooftop_accuracy.py` runs three CI-friendly sanity checks against the rooftop pipeline output: (1) per-sector capacity-vs-MWp/kt z-score band (flag |z| > 2σ for sectors with ≥4 sites), (2) zero-MWp-but-nonzero-capacity explicit rule (catches the RV7 GoB-undercount pattern that z-scores miss because zero pulls the median), (3) plant-area band (footprint should be 5–30% of polygon for standalone/cluster sites with a fence boundary). Console output grouped by flag type with self-explanatory headers (rule + fix); JSONL trend log at `outputs/data/eval/rooftop-history.jsonl`; exit 1 on actionable findings for CI. Complements the spec §16 manual ±20% pass — automated flags surface the right sites for the manual sample. Cross-source IoU agreement is the fourth planned check, deferred until Microsoft GMLBF lands. Constants in `src/assumptions.py::EVAL_*`. 18 unit tests at `tests/test_eval_rooftop_accuracy.py`. First run on the current pipeline surfaces 11 actionable findings: 7 zero-MWp cases (Hongshi, Cemindo Bayah, IWIP, IPIP, Buli, PIM, SBI Andalas Lhoknga — all known RV7), 4 sector_outlier_above (Semen Gresik City at +4.6σ surfaces residential bleed despite filter, plus Semen Kupang, Bantaeng IIA, Jakarta Prima Steel). Plus 5 review-tier footprint-band hits (Indocement Palimanan + Krakatau Posco under-detected, Gunung Raja Paksi + Semen Baturaja over the ceiling). Methodology §4A.8.
- **V4.1 industrial site polygons — fence-boundary clip for non-KEK plants.** New file `data/industrial_sites/site_polygons.geojson` captures `landuse=industrial` / `man_made=works` polygons from OSM Nominatim for 9 plants where they're well-mapped: Indocement Palimanan, Cemindo Gemilang Bayah, Petrokimia Gresik, Ispat Indo Sidoarjo, Freeport Smelter Gresik, Semen Padang Indarung, Semen Baturaja, Krakatau Posco Cilegon, Gunung Raja Paksi Bekasi. The same KEK polygon-clip mechanism in `load_buildings_for_pipeline()` is now generalized to consume both KEK and industrial polygons via `_load_site_polygons()` + `_clip_buildings_to_site_polygons()` (renamed from the KEK-only versions). Sites without a polygon pass through unchanged with the 2 km preprocess buffer + RV11 residential-pattern filter as the catchment lever. Side effect: 3 OSM polygons revealed wrong dim_sites centroids (Ispat Indo 10 km off, Freeport 9.8 km off, Krakatau Posco 3 km off) — those polygon centroids were then captured as additional rows in the coordinate-override CSV. Methodology §4A.4 updated. See `scripts/visual_sanity_check.py` to verify any site visually.
- **V4.1 rooftop solar potential — pipeline, classifier, and visual tile layer (Phase 1–3).** New `fct_site_solar_potential.csv` (81 rows × 18 columns) computes a per-site rooftop nameplate ceiling from Google Open Buildings v3 (vintage 2023-05). The §14 classifier in `src/model/buildings.py` filters detected polygons by circularity, aspect ratio, and convex-hull ratio into 7 categories (`standard_roof` 1.0 → `tank_silo` 0.0) before applying the §5.3 footprint→MWp math. Pipeline architecture follows spec §13.10 invariant 4: `load_buildings_for_pipeline()` in `src/pipeline/build_fct_site_solar_potential.py` is the single place buildings enter the pipeline; both the aggregator and the new tile generator (`build_rooftop_tiles.py`) read through it. Visual layer: 703,001 panel-tile polygons (~25 MB parquet) grouped into 9,622 DBSCAN clusters (eps=50m) feed the "Rooftop Solar Tiles" map overlay with click-to-popup for the show-your-work loop (spec §3.6.1). Frontend additions: `LayerControl.tsx` toggle for the tile layer, portal-rendered cluster popup with design-system CSS variables, satellite map style as the default. Methodology written up at METHODOLOGY §4A; data dictionary entry at §3.12; full spec at `docs/rooftop_solar_potential_feature_spec.md`.
- **V4.1 KEK polygon clip (V3.11.1).** `load_buildings_for_pipeline()` now applies a centroid-within-polygon filter against `outputs/data/raw/kek_polygons.geojson` for the 25 KEK sites — the upstream 2 km buffer in `preprocess_open_buildings.py` over-included residential structures and adjacent industrial parks (notably for irregularly-shaped KEKs like Tanjung Sauh's 6-island fragments). Industrial sites (cement, steel, nickel) have no polygon and pass through unchanged; their 2 km buffer IS the intended catchment for now (TODOS RV8 tracks per-sector radii). Result: 11,471 buildings dropped (90,643 kept), KEK rooftop total falls from ~170 MWp → 58.9 MWp; industrial total unchanged at 1,727 MWp. Both consumers (aggregator + tile generator) pick up the clip automatically since they share the loader.

### Fixed
- **Map crash on zoom (V4.1 Phase 3).** Hovering a rooftop tile polygon while `interactiveLayerIds` registered both `kek-circles` (Point) and `rooftop-tiles-fill` (Polygon) caused `handleMouseEnter` to dereference `(feature.geometry as GeoJSON.Point).coordinates` on a Polygon, producing `NaN` lng/lat that threw inside `Popup.setLngLat` and unmounted the entire map subtree (black screen). Hover handler now gates on `layer.id === 'kek-circles'` before touching `geometry.coordinates`. Belt-and-braces: viewport-clipped feature rendering (`visibleRooftopTiles` useMemo with precomputed centroids in `feature.properties._lng/_lat`) caps the active tile count for the GeoJSON source even on 77k-tile sites.
- **Classifier calibration on Semen Imasco Asiatic Jember.** The previous `BUILDING_HULL_RATIO_COMPLEX_THRESHOLD = 0.70` and `BUILDING_ASPECT_CONVEYOR_THRESHOLD = 8.0` were dropping a legitimate ~9,309 m² production hall into `complex` (multiplier 0.2) and a ~4,310 m² hall into `conveyor` (multiplier 0.1). Loosened to 0.55 and 15 respectively to reflect that real industrial halls are often long-and-rectangular without being conveyors. Calibration tied to that specific site — broader manual validation pass deferred to Phase 4 (TODOS).

- **Phase 0 prerequisite for v4.1 rooftop solar — column rename + `Col` enum scaffolding (M23 starts).** The `max_captive_capacity_mwp` column is renamed to `regional_groundmount_potential_mwp_50km` everywhere in the pipeline + dash logic + API + frontend. Old name was misleading ("captive" suggested on-site; the number is regional 50 km ground-mount potential). API serves both names for one release via a deprecation alias in `src/api/routes/scorecard.py::_df_to_clean_records`; old name removed in v4.2. **Also kicks off M23 (`Col` enum class for column constants):** new module `src/model/columns.py` houses canonical column names as class attributes. The renamed column is the first user — `Col.REGIONAL_GROUNDMOUNT_POTENTIAL_MWP_50KM`. Migrating other columns to `Col` is incremental in future PRs (per the original "one pipeline file at a time" plan). Files updated: 9 src files (pipeline + dash logic + API), 5 frontend files, 6 test files, 11 doc files. New regression test `tests/test_api.py::test_scorecard_max_captive_capacity_mwp_alias` verifies the alias pairing for one-release backward compat. Frontend `ScorecardRow.max_captive_capacity_mwp` marked `@deprecated` in `types.ts`. All 618 tests green; type-check clean. Unblocks the v4.1 rooftop solar feature ([docs/rooftop_solar_potential_feature_spec.md](docs/rooftop_solar_potential_feature_spec.md) §3.1 F5).
- **V3.11 (M31) `CostBasis` user toggle — Raw / Firmed / Delivered.** Three-way segmented control in the header that repoints `action_flag`, `economic_tier`, `solar_competitive_gap_pct`, and `carbon_breakeven_usd_tco2` at the cost-stack tier the active persona cares about (T1 raw generation LCOE, T2 firmed LCOE with BESS adder, T3 delivered captive+grid blend). Default basis preserves pre-M31 behaviour: `firmed` for `overall` energy mode, `raw` otherwise. Unsupported `(mode × basis)` cells (e.g. wind × delivered) render the toggle option disabled with a "Not modelled for {mode}" tooltip — no silent fallback. Resolver matrix documented in METHODOLOGY §10.5 and TAXONOMY §7.3. New backend: `CostBasis` StrEnum in `basic_model.py`; 5 enum invariants tests in `tests/test_cost_basis_enum.py`; `enrich_grid_passthroughs` now surfaces `grid_emission_factor_t_co2_mwh` on every scorecard row so the frontend carbon-breakeven re-derives against the real EF per basis. New frontend: `frontend/src/lib/costBasis.ts` resolver + `isBasisSupported` / `defaultCostBasis` helpers, `CostBasisToggle` (Radix ToggleGroup + Tooltip.Provider), Zustand `costBasis` slice + auto-flip when the current basis goes unsupported on an energy-mode switch. Callsites respecting `costBasis`: `getEffectiveActionFlag` / `getEffectiveEconomicTier` / `getEffectiveCarbonBreakeven` in `actionFlags.ts`, map markers, `EconomicTierCell` + `ReAssessmentCell` + economic-tier facet filter in the data table, CSV export metadata, ScoreDrawer header + Action tab, QuadrantChart x-axis. Golden fixture regenerated (81 × 142 → 81 × 143 cols). Persona payoff: energy economist picks `(solar, raw)` to ask "can the cheapest MWh beat BPP?"; industrial investor picks `(solar, delivered)` to ask "what will the tenant actually pay?"; DFI picks `(*, firmed)` to ask "what's the firm-power price?". One toggle, three answers, zero methodology debate.
- **V3.10 blended delivered cost metric** — tenant-view cost of electricity when on-site solar partially covers demand. New enricher `enrich_delivered_cost()` in `src/dash/logic/scorecard.py` computes `f_captive × LCOE_wb + f_grid × grid_rate` using the haircut-adjusted within-boundary coverage (same figure the `within_boundary` gate checks). Fills the partial-coverage gap between the binary gate outcomes: a KEK with 15% effective captive coverage no longer reports the full grid-connected LCOE as if on-site solar didn't exist. Six new scorecard columns: `delivered_cost_usd_mwh`, `captive_fraction`, `grid_fraction`, `delivered_cost_grid_rate_used_usd_mwh`, `delivered_cost_wb_lcoe_used_usd_mwh`, `delivered_cost_gap_vs_grid_pct`. Additive — `lcoe_mid_usd_mwh`, action flags, and economic tier are unchanged. Concrete impact on partial-captive KEKs: Galang Batang moves from +58.8% above-grid on `lcoe_mid` to +4.5% on delivered. Methodology §5.4 documents the formula + worked example; 8 unit tests (`tests/test_logic_scorecard_delivered_cost.py`). Golden fixture regenerated (81×136 → 81×142). TODOS M31 tracks the open question of repointing downstream mechanics (action flags, economic tier, carbon breakeven) at the blended number.
- **Rename `delivered_cost_blended_usd_mwh` → `delivered_cost_usd_mwh`** (TAXONOMY §7.1). "Blended" was redundant — delivered cost *is* the captive+grid blend. Swept scorecard.py, test fixture, types.ts, columns.tsx, EconomicsTab.tsx, METHODOLOGY §5.4, DATA_DICTIONARY, TAXONOMY active tables. Historical §9/V3.10 diary entries left frozen. Unblocks M31 `CostBasis` resolver from baking in a name that would change.

### Changed
- **Design polish pass — dashboard chrome, typography, wayfinding.** Consolidates ~25 visual and interaction fixes surfaced by `/design-review` and `/plan-design-review`. Header: bumped h1 from 14 → 18px bold + tight tracking so the page title actually reads as a heading instead of blending with body text; swapped persona emoji for monochrome SVG icons (`WalkthroughModal.tsx`); dropped "Blended" from the Supply Blend cost-basis labels; one-font-stack via `--font-sans` CSS var (no more mixed Inter / system defaults). Legend surface: collapsed the old floating-card `RasterLegend` into a 32px glass strip that sits flush below the Header via a flex column — eliminates the "card lands on top of the map + Back button" double-stack, and the flex wrap removes the magic-number top offsets that broke whenever padding or typography shifted. "Back to National View" button: moved below the new strip (`top-[106px]`), shrunk from `px-5 py-2 text-sm rounded-xl` to `px-3 py-1 text-xs rounded-lg` so it reads as secondary to the strip it now orbits. Ranked Table: dropped font `text-sm → text-xs`, padding `px-3 py-2 → px-2 py-1`, and filter-row `py-1 → py-0.5` — ~33% more rows visible per scroll without feeling cramped. Map canvas: new `MapHint` component shows a subtle "81 sites across Indonesia · Click a marker to explore" pill once on first landing (dismissed forever in `localStorage.map_hint_seen` the moment the user clicks any marker). Bottom panel: overlays map instead of pushing it, no more 18-frame MapLibre re-tile on toggle; inactive tabs lazy-mount so RuptlChart / SectorSummaryChart / ScenarioCompareTab don't pay their Recharts+ResizeObserver startup cost until the user actually opens them. Scenario Compare: the tier/infra chips in `FlipDiffTable` now `whiteSpace: 'nowrap'`, Compute Flip disabled-state is dashed-accent with an inline "(pick a preset)" caption, and duplicate empty-state in `FlipSummary` removed. Sector Summary chart: y-axis ticks snap to nice round numbers via a new `niceTicks()` helper. Ranked Table filter badge on the rail now hides when `filteredSiteIds` matches the full scorecard (previously showed `81` misleadingly all the time).
- **Performance polish.** Lazy-mounted `<Tabs.Content>` children in `BottomPanel` (Radix renders all by default — that was spinning up three Recharts ResizeObservers no one was looking at). Stopped actively resizing the map canvas when the bottom panel opens/closes — panel is now a glass overlay, canvas dimensions are stable, zero MapLibre re-tiles on toggle. Stabilized the zoom handler that was re-binding on every tab/collapse state change. Auto-collapses Ranked Table when the user zooms past the regional threshold (they're inspecting, not browsing).

### Fixed
- **Latent `enrich_cbam` grid emission factor bug.** `enrich_cbam` in `src/dash/logic/cbam.py` read `row.get("grid_emission_factor_t_co2_mwh")` to compute CBAM Scope 2 savings, but the field was never written to the row — the `or 0.8` fallback fired for every site. 45 of 81 sites sit in grid regions with non-default EFs (0.56 Papua → 1.27 NTB per KESDM Tier 2 OM); 35 of those are CBAM-exposed and were silently mis-costed against the Java-Bali average instead of their own grid's carbon intensity. Surfacing `grid_emission_factor_t_co2_mwh` in `enrich_grid_passthroughs` (pos 2, before `enrich_cbam` at pos 13) closes the lookup. Golden fixture regenerated — affected sites' `cbam_savings_2026/2030/2034_usd_per_tonne` shifted in both directions (higher savings in Sumatra/NTB, lower in Papua).
- **Substation capacity traffic-light false positives.** `capacity_assessment()` classified ratios between 1.0 and 2.0 (available real power / solar MWp) as yellow "Marginal" and surfaced a "Why upgrade? Transformers need uprating to absorb the surplus" panel in the ScoreDrawer Grid tab — even though the project fit entirely within the available throughput and `substation_upgrade_cost_per_kw` correctly returned $0. User-reported case: 28 MWp solar vs 36 MW available (42 MVA × 0.85 PF) wrongly flagged as needing an upgrade. Bands now aligned with the cost function: green = ratio ≥ 1.0 (fits, no uprating), yellow = 0.5–1.0 (partial overflow), red = < 0.5 (major deficit). Traffic light and cost column can no longer contradict each other. Golden-master scorecard regenerated — several sites flipped yellow → green.

## [1.3.0] - 2026-04-20

V3.7 substation-anchored solar picker fixes "Build Substation" false positives + chosen-polygon highlight + pre-landing review cleanups.

### Added
- **V3.7 substation-anchored solar search** (`src/pipeline/build_fct_site_resource.py`). Replaces the argmax-PVOUT pixel picker in `_compute_buildable_pvout()` with a multi-step search that prefers buildable patches sitting near existing substations. Algorithm: compute `required_mwp = demand_mwh / (8760 × cf_centroid)`, set `meaningful_mwp = 0.30 × required_mwp`, enumerate candidate patches within `ANCHOR_SEARCH_RADIUS_KM = 10 km` of every substation within a region-tiered KEK radius, keep candidates meeting the meaningful-share floor, rank by LCOE-proxy (`lcoe_solar(cf_candidate) + annualised gen-tie cost`), and fall back to argmax PVOUT only when no candidate qualifies.
- **Geography-tiered search radius.** `KEK_TO_SUBSTATION_RADIUS_BY_REGION_KM` in `src/assumptions.py`: JAMALI 15 / Sumatra 25 / Kalimantan 30 / Sulawesi 30 / Maluku-Papua 40. Recognises that dense Java grid permits a tight radius while eastern Indonesia's sparse grid needs more reach. Helper `_search_radius_km()` dispatches on both dim_sites `grid_region_id` values (JAVA_BALI, SUMATERA, etc.) and PLN `regpln` values (Jawa-Bali, Sumatera, etc.) with graceful fallback for unknown regions.
- **Voltage-class hosting-capacity proxy.** When PLN `kapgi` (nameplate MVA) is missing for a substation, infer from `teggi` voltage class: 500kV → 500 MVA, 275kV → 250, 150kV → 60, 70kV → 20, 20kV → 5. Apply `HOSTING_CAPACITY_AVAILABILITY_PCT = 0.30` (conservative upper bound for proxied rows only — actual kapgi rows use their real value). New column `nearest_substation_capacity_source` records whether the capacity came from `actual` kapgi or a `proxy_<voltage>` inference.
- **CF-corrected delivered-share column.** New `solar_delivered_share_pct = (nameplate_mwp × 8760 × cf_anchored) / demand_mwh` distinguishes the anchored patch's actual annual energy delivery from its nameplate supply share (`solar_supply_share_pct`). Surfaces in ScoreDrawer Grid tab tooltips.
- **Solar regulatory regime + Perpres 112/2022 ceiling.** New `solar_regime` enum (`co_located_captive` / `grid_connected_ipp` / `unclear`) on the scorecard, derived from within-boundary coverage + grid integration category + Perpres-112 exempt sectors. New `lcoe_grid_connected_capped_usd_mwh` applies `PERPRES_112_CEILING_USD_MWH = 75.0` as a ceiling when regime is `grid_connected_ipp`, reflecting the regulated IPP tariff cap. Captive and unclear regimes bypass the ceiling.
- **New scorecard columns:** `solar_search_method`, `chosen_anchor_substation_name`, `solar_supply_share_pct`, `solar_delivered_share_pct`, `project_scale_solar_mwp`, `nearest_substation_capacity_source`, `solar_regime`, `lcoe_grid_connected_capped_usd_mwh`.
- **ScoreDrawer Grid tab V3.7 visibility.** New "Solar Site Selection" card surfaces the picker method, chosen anchor substation, project scale, demand coverage (supply + delivered), and capacity source when a hosting-capacity proxy was used.
- **21 new tests in `tests/test_substation_anchored_search.py`.** Five classes covering `required_solar_mwp`, search-radius dispatch (5 regions + regpln + unknown fallback), anchored picker happy path (near wins; lowest LCOE beats farther), fallback paths (no subs in range; demand too large; empty input), and regime/ceiling application (captive + IPP + unclear + standalone).
- **Chosen-polygon amber highlight on map.** When a site is selected, the buildable polygon the V3.7 picker chose lights up amber (`#FFD54F` fill, `#FFA000` outline) so the user can see exactly which patch powers the LCOE quote. Implementation: `src/dash/map_layers.py::load_buildable_polygons` injects a stable `feature_index` into every polygon; `src/dash/logic/scorecard.py` seeds `best_solar_site_lat/lon` on every row; frontend `frontend/src/lib/map/pointInPolygon.ts` resolves row coords → feature_index via ray-cast point-in-polygon with nearest-centroid fallback; `frontend/src/components/map/VectorOverlay.tsx` renders two conditional `<Layer>` children under the buildable `<Source>` (unwrapped fragment — `react-map-gl`'s `<Source>` iterates via `React.Children` which does not flatten fragments, so the prior fragment-wrapped version rendered nothing).
- **ModifierBadgePill hover tooltip.** CBAM-urgent / plan-late / storage-info badges in the ScoreDrawer header and ActionTab now reveal the full badge description on hover via a portal-rendered tooltip (`frontend/src/components/ui/ModifierBadgePill.tsx`). Two size variants (sm / md) for header vs ActionTab placement.
- **"KEKs only" DataTable filter.** New toggle in the table toolbar composes with the CBAM-exposed filter (AND semantics), letting users focus on the 25 KEK sites without losing other active filters.
- **New scorecard columns for polygon highlight:** `best_solar_site_lat`, `best_solar_site_lon`, `within_boundary_coverage_pct`.

### Changed
- **Three-point proximity inputs.** `grid_integration_category()` contract is unchanged, but its inputs (best solar lat/lon, solar-to-substation distance) now reflect the V3.7 anchored pick. Existing `TestGridIntegrationCategory` suite passes unmodified — 22/22 green.
- **Golden-master fixture.** `tests/fixtures/scorecard_golden.pkl` regenerated to 81 rows × 124 cols (was 114 cols). 10 new V3.7 columns captured as the new baseline (8 picker + anchor columns plus `best_solar_site_lat`/`best_solar_site_lon` / `within_boundary_coverage_pct` for the polygon highlight).

### Fixed
- **Batam Aero Technic "Build Substation" false positive.** The site sits 3.96 km from GI 150 kV Nongsa 1, but the argmax-PVOUT picker selected a pixel ~50 km away in Bintan with nearest sub GI 150 kV Tg. Kasam at 17.9 km — triggering `invest_substation`. Post-V3.7 the anchored picker selects a 14.89 MWp patch 1.22 km from Nongsa 1, the site's `grid_integration_category` flips to `grid_ready`, and the dashboard shows "Grid Ready" with the correct anchor substation.
- **CRITICAL: spurious `× 1000` in `solar_delivered_share_pct` formula** (`src/pipeline/build_fct_site_resource.py:604,902`). Previous formula `candidate_mwp × 1000 × 8760 × cf / demand_mwh` silently treated nameplate MW as kW, producing kWh on the numerator vs. MWh in the denominator — `solar_delivered_share_pct` always pegged at the 1.0 cap. Fixed to `candidate_mwp × 8760 × cf / demand_mwh`. Pipeline regen required to refresh live values (golden fixture regenerated).
- **HIGH: `cf_for_sizing` NaN fallback in substation proximity builder** (`src/pipeline/build_fct_substation_proximity.py:334-429`). When `cf_centroid` was NaN due to raster holes (cloud gaps, coastal pixels), the sizing calc fell through to 0 and the hosting-capacity check misfired into "Build Substation" — the exact regression V3.7 was meant to prevent. Now falls back to `pvout_buildable_best_50km / 8760` when centroid CF is missing.
- **HIGH: stale mode-dependent labels in DataTable cells** (`frontend/src/components/table/columns.tsx`). Three cells (`GridIntegrationCell`, `EconomicTierCell`, `InfrastructureReadinessCell`) were reading `useDashboardStore.getState().energyMode` inside render — that's a one-shot read, not a subscription, so toggling solar/wind/hybrid/overall did not re-render the rows. Extracted into component subscribers via the `useDashboardStore((s) => s.energyMode)` hook.

### Changed
- **`SUBSTATION_COLOCATION_RADIUS_KM` split into two constants.** The old single 10 km value did double duty: the V3.7 picker's co-location search radius AND the captive-vs-grid-connected regime cutoff. Tuning one silently moved the other. Now `ANCHOR_SEARCH_RADIUS_KM` (picker) and `CAPTIVE_REGIME_MAX_KM` (regime) in `src/assumptions.py`. Both start at 10.0 so current behaviour is unchanged; divergence now requires an explicit decision.
- **Deduplicated haversine_km in substation proximity builder** (`src/pipeline/build_fct_substation_proximity.py`). Removed the private `_haversine_km` and the now-unused `import math`; now imports the shared implementation from `src.pipeline.geo_utils`.

### Tests
- **Region→tier dispatch coverage** (`tests/test_region_tier_dispatch.py`, 3 tests). Asserts every `grid_region_id` in `dim_sites.csv` and every `regpln` in `substation.geojson` resolves to a non-default search-radius tier. Guards against silent fallback to the 15 km default when a new region is added to data but not to `_REGION_TO_TIER_KEY`.

### Docs
- `docs/METHODOLOGY_CONSOLIDATED.md` — updated §8.1 three-point inputs for V3.7; new §8.7 covers the full algorithm, thresholds, hosting-capacity proxy, regulatory regime, Perpres ceiling, and Batam verification.
- `DATA_DICTIONARY.md` — §3.1 (fct_site_resource) adds 5 new column rows; §3.4 (fct_substation_proximity) adds 2; §3.6 (fct_site_scorecard) adds `solar_regime` and `lcoe_grid_connected_capped_usd_mwh`.
## [1.2.0] - 2026-04-19

Scenario Compare + CBAM methodology rewrite + 81-site universe + Persona 6.

### Added
- **Scenario Compare — A/B flip vs baseline.** New `compare` bottom-panel tab runs a second scorecard under a flip scenario (e.g. concessional finance, higher CBAM price, grid fix) and diffs against baseline. Per-site delta LCOE, gap-to-grid delta, tier transitions, and flip counts.
- **Scenario Compare — Phase 5: map flip rings + per-site Flip tab.** Sites whose economic tier moves under the flip scenario now wear a coloured halo on the map (green = improved, red = worsened) sized just outside the CBAM amber ring. A second `kek-flip-pulse` layer pulses continuously (radius 14→26 px over 2 s, opacity 0.85→0) like a Google-Maps active-location indicator so flipped sites read at a glance. Both layers are gated on `activeTab === 'compare' && flipScorecard != null` and powered by `requestAnimationFrame`; cleanup zeros the paint properties when Compare is closed. New 7th `Flip` tab in `ScoreDrawer` (`scoredrawer/FlipTab.tsx`) shows per-site scenario banner with direction chip, tier transition (baseline → flip), delta LCOE, gap-to-grid delta, and CBAM-urgency before/after with NEW / CLEARED chips. Empty state CTA jumps the user to the Scenario Compare bottom tab when no flip has been computed yet.
- **ScoreDrawer tab bar fits 7 tabs at 420 px.** Reduced tab-list padding (`px-4 gap-0.5` → `px-3 gap-0`) and per-tab padding (`px-2.5` → `px-1.5`); added `whitespace-nowrap flex-shrink-0` and `overflow-x-auto` as a safety net. Previously the 7th `Flip` tab was clipped off the right edge of the drawer.
- **Sector pictogram map markers.** Every site marker now shows a white SVG pictogram inside the coloured econ-tier disc — steel anvil, cement kiln, ammonia flask, petrochem column, nickel factory, aluminium ingots, fertilizer sack, KEK/mixed skyline. Single path source in `frontend/src/lib/sectorIcons.ts` shared by map (SDF layer via MapLibre `icon-color` tinting) and `ActionFlagLegend.tsx` (new "Icon = Sector" section) so they can't drift. CBAM amber ring sized just outside the marker disc.
- **Action-flag hover tooltips.** Every action flag in the ScoreDrawer Action tab now carries a hover tooltip explaining what it means and what moves a site off that flag.
- **Ammonia + petrochemical scaffolding (no rows yet).** `Sector` enum extended with `ammonia` and `petrochemical`; demand intensities and reliability defaults wired into `src/pipeline/demand_intensity.py` and `src/model/site_types.py`. CBAM cost model calibrated for ammonia using Indonesia-specific Scope 1 = **2.3 tCO₂/t** (ICGD gas-SMR — higher than the legacy aggregated "fertilizer" 1.2 tCO₂/t); ammonia is CBAM-exposed via CN 2814. Petrochemical is intentionally **NOT** in EU CBAM Annex I.
- Frontend rollup colors: `SectorSummaryChart.tsx` reserves teal (#26A69A) for ammonia and purple (#7E57C2) for petrochemical.
- TODOS M28 (ammonia) and M29 (petrochemical) opened to drive completeness via top-down universe discovery (state-holdings + industry-association + government-filing + trade-stat intersection) instead of ad-hoc hand-picked rows.
- **Fertilizer universe closure (M26 complete, 2026-04-18).** 4-source universe-discovery gate executed → `data/industrial_sites/fertilizer_universe_v1.csv` (7 candidates: all 5 operating Pupuk Indonesia subsidiaries + Fakfak under-construction + Multi Nitrotama Kimia flagged ammonium-nitrate out of CBAM scope). Added the 2 previously-missing Pupuk sites — **Pupuk Kujang (Cikampek, West Java, 1.14 Mt/yr urea)** and **Pupuk Iskandar Muda (Lhokseumawe, Aceh, 1.14 Mt/yr urea)** — to `priority1_sites.csv` with source URLs. Provenance per row enforced by loader.
- **CBAM `CBAM_RE_ADDRESSABLE_FRACTION` fix (M30 complete, 2026-04-18).** New dict in `src/assumptions.py` (cement 0.12, fertilizer 0.10, ammonia 0.10, steel_bfbof 0.80, everything else 1.0). Wired into `src/dash/logic/cbam.py::compute_cbam_trajectory`: Scope 2 RE savings now multiplied by the sector's electric share of thermal-inclusive intensity values. Scope 1 path untouched. 4 new tests in `tests/test_logic_cbam.py` lock behaviour. Golden-master fixture regenerated.
- **Walkthrough copy refresh + WACC auto-adjust.** Economist, DFI Investor, and Policy Maker walkthroughs re-grounded in current metrics: 81 sites (25 KEK + 56 industrial), 68/81 CBAM-exposed, Papua \$133/MWh vs Java \$57/MWh BPP spread, M30 RE-addressable fractions, 97.5% → 0% free-allocation phase-out. The "WACC at 8%" demo step now auto-sets the assumptions slider via `setAssumptions({ wacc_pct: 8 })`, and the final step reminds the user to Reset.
- **Bottom panel default height + drag discoverability.** Default ranked-table height grows 448 → 580 px so more rows show out of the box. Tailwind `group-hover:` widens the resize-grip pill and reveals an "↕ Drag to resize" label on hover, making the previously invisible drag handle discoverable.
- **CBAM methodology §14 rewrite: Energy-vs-Process framing.** `docs/METHODOLOGY_CONSOLIDATED.md` §14.2 now leads with the plain-language two-bucket mental model — energy emissions (Scope 2, solar-addressable) vs process emissions (Scope 1, chemistry-bound). §14.3 shows the code's formula first, then worked examples (nickel RKEF ~89% cut with solar, cement ~7% cut because it's mostly calcination), then the M30 RE-addressable fraction. Explains why cement savings are $7.6/t not $63/t.
- **Persona 6: Green Industry Roadmap Planner.** BKPM/KESDM/Bappenas/sustainability-consultancy user documented in `PERSONAS.md` as the primary beneficiary of the V4.1 expansion. User journey: Sector Summary chart → sector filter → ScoreDrawer → Scenario Compare flip → CBAM trajectory → CSV → Zenodo DOI citation. Readiness 87% (pathway_flag + sectoral PDF export + M28/M29 remain gaps).
- **Priority 6 export spec stashed.** `docs/PLAN_EXPORTS_PRIORITY6.md` captures the sectoral summary PDF + flip diff CSV rollups design, scoping Qs, and revisit triggers. Not yet built.

### Changed
- **Pipeline-of-enrichers refactor (`src/dash/logic/scorecard.py`).** `compute_scorecard_live` split from a 720-LOC monolith into a `STAGES` list of enrichers + a thin per-site loop. Each stage owns one concept (lcoe, cbam, grid, technology) and receives a `SiteContext` scratchpad. External API preserved via `src/dash/logic/__init__.py` re-exports. Pickle golden-master confirms bit-identical output.
- **Performance quick wins.** gzip response compression on the API, Vite chunk splitting for the frontend, and a fix for a zoom-lag handler that was remounting map layers on every render. First-paint and map interactions noticeably snappier on slower connections.

- Site count **79 → 81** (25 KEK + 46 standalone + 10 cluster). Fertilizer rows 3 → 5 — the full Pupuk Indonesia Group operating fleet now covered.
- `fct_lcoe` rows **1,422 → 1,458** (81 × 9 WACC × 2 scenarios).
- CBAM exposed sites **66/79 → 68/81** (12 KEK 3-signal + 56 industrial direct: 32 cement + 17 iron_steel + 5 fertilizer + 2 aluminium).
- Cement/fertilizer/ammonia CBAM savings drop sharply (cement 2034 savings 63 USD/t → 7.6 USD/t) because Scope 2 savings are now bound to the electric share of thermal-inclusive intensity values. CBAM cost itself is unchanged.
- Test count **537 → 541** (4 new CBAM fraction tests).

### Fixed
- **WalkthroughModal unmount/remount loop.** When a walkthrough auto-action mutated state (e.g. WACC slider step), the global `loading` flag flipped true mid-recompute, the modal returned `null`, and remounting re-fired the action effect — kicking off another recompute and looping. Guard now reads `loading && !scorecard` so it only short-circuits during the initial pre-scorecard load.

## [1.1.0] - 2026-04-17

V4.1 Industrial Parks Expansion + internal refactors.

### Added
- **Industrial parks expansion: 25 KEKs → 79 sites** (32 cement + 7 steel + 10 nickel IIA clusters + 2 aluminium + 3 fertilizer added)
- Tracker-driven site selection (`build_industrial_sites.py`) reads GEM Global Cement/Iron-and-Steel Plant Trackers and CGSP Nickel Tracker; cement/steel/nickel rows generated programmatically with explicit filter rules; only aluminium + fertilizer remain residual manual rows (with required `source_url`)
- Site-types registry (`src/model/site_types.py` + `frontend/src/lib/siteTypes.ts`) — single `SITE_TYPES` dict drives per-type demand estimation, captive power matching, CBAM detection, and marker shape; mirrored in TypeScript for the frontend
- Dual-mode dispatch via `SiteTypeConfig`: KEKs/KIs use area-based demand + proximity captive matching + 3-signal CBAM; standalone/cluster sites use sector-intensity demand + direct captive matching + direct CBAM via `cbam_product_type`
- Sector summary chart (`SectorSummaryChart.tsx`) — sector-aggregated CBAM cost + 2030 demand + action-flag distribution
- Site-type and sector dropdown filters in the data table; registry-driven identity card in ScoreDrawer (renders fields per `SiteTypeConfig.identityFields`)
- CI workflow: pytest + ruff lint + TypeScript type-check on every push and PR
- CONTRIBUTING.md with dev setup, testing, and PR process
- CSV export now includes assumptions, thresholds, energy mode, benchmark mode, and export date
- RUPTL Context chart connects to selected site: highlights grid region, shows post-2030 % with plan_late badge, 2030 reference line
- Bottom panel drag-to-resize (200-700px range)
- Golden-master test (`tests/test_scorecard_golden.py`) locks `compute_scorecard_live` output against a fixture pickle so future refactors can verify bit-identical behavior

### Changed
- **`src/dash/logic.py` (1,437 LOC) split into `src/dash/logic/` package** by domain: `assumptions.py`, `lcoe.py`, `cbam.py`, `grid.py`, `technology.py`, `scorecard.py`. Public API preserved via `__init__.py` re-exports — external callers unchanged.
- **`frontend/src/components/panels/ScoreDrawer.tsx` (2,244 LOC) split into `scoredrawer/` folder** (9 files): one file per tab (Overview/Resource/Grid/Economics/Demand/Action), shared `StatComponents.tsx`, `IdentityCard.tsx`, `formatting.ts`. Shell file is now 237 LOC.
- Renamed `kek_id` → `site_id` and `/api/kek/{id}/...` → `/api/site/{id}/...` across backend, frontend, and tests for consistency with the broader site-type model
- Renamed pipeline outputs `fct_kek_*` → `fct_site_*` (resource, demand, scorecard, wind_resource)
- 532 tests now pass (up from 422); added module-boundary tests for each new logic submodule + site-types/geo-utils/demand-intensity tests

### Fixed
- Hybrid LCOE floor: optimizer now returns standalone wind when no hybrid mix can beat it (was adding BESS to 100% wind unnecessarily)
- Wind energy balance chart now renders in wind mode (was only showing in hybrid mode)
- QuadrantChart tooltip now reads correct grid cost in BPP mode (was showing tariff value)
- Show/Hide toggle centered in both collapsed and expanded states
- `data_loader.prepare_resource_df()` now merges `site_type`/`cbam_product_type`/`technology`/`capacity_annual_tonnes` from `dim_sites` onto resource_df so industrial sites correctly route through direct CBAM detection (previously fell through to 3-signal and showed $0)

### Removed
- Quadrant Chart tab disabled (file retained for future use)
- `src/dash/logic.py` monolith (split into package; legacy file deleted)

## [1.0.0] - 2026-04-14

Initial public release.

### Added
- LCOE model for solar, wind, and hybrid (solar+wind) with BESS storage
- 25 KEK scorecard with action flags (solar_now, invest_transmission, invest_substation, grid_first, invest_battery, invest_resilience, plan_late, not_competitive, no_solar_resource)
- 5-layer solar buildability filter (kawasan hutan, peatland, land cover, road proximity, slope/elevation)
- 6-layer wind buildability filter with buildable polygon overlay
- Grid integration: 3-point proximity, PLN substation capacity traffic lights, grid line connectivity
- CBAM exposure analysis: 3-signal detection (nickel process, plant counts, KEK sectors), cost trajectories 2026-2034, 12/25 KEKs exposed
- Captive power overlays: coal (GEM), nickel (CGSP), steel (GEM), cement (GEM)
- React + Vite + TypeScript frontend with MapLibre GL map, liquid glass UI
- FastAPI backend serving precomputed pipeline data
- Energy mode toggle: Solar / Wind / Hybrid / Overall
- Benchmark toggle: BPP (cost of supply) vs I-4 industrial tariff
- User-adjustable assumptions panel with 3-tier sliders
- Persona-driven walkthroughs (Economist, DFI Investor, Policy Maker, IPP Developer, Industrial Investor)
- Scenario save/load with URL sharing
- RUPTL pipeline context chart by grid region
- Ranked table with dropdown/range filters, CBAM filter, CSV export
- ScoreDrawer with 6 tabs (Resource, Grid, Economics, Action, CBAM, Energy Balance)
- Distance measurement tool on map
- Auth gate for deployment
- Docker + Render deploy config
- 422 tests across model, pipeline, and API modules
- Zenodo DOI for citation
