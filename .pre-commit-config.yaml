repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.0
    hooks:
      - id: ruff            # lint + auto-fix (imports, unused vars, etc.)
        args: [--fix]
      - id: ruff-format     # format (black-compatible)
